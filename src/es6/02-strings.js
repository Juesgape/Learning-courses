let hello = 'Hello'
let world = 'World'
//Template literals
let epicPhrase = `${hello} ${world}!`

console.log(epicPhrase)

// Multi-line Strings

let lorem = `This is an epic phrase
and this is its continuation`;

console.log(lorem)